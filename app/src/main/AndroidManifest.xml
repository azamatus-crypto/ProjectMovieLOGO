<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.base.projectmovielogo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/moviepnglogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectMovieLOGO">
        <activity android:name=".WEbWeiewEnglish"></activity>
        <activity android:name=".WEbWeiewActivity" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".DeatailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>